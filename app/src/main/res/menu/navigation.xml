<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/navigation_purchase"
        android:icon="@drawable/ic_purchased_24dp"
        android:title="Purchases"
        android:checked="false"
        app:showAsAction="always"/>

    <item
        android:id="@+id/navigation_group"
        android:icon="@drawable/ic_groupicon_24dp"
        android:title="Groups"
        android:checked="false"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/navigation_watchlist"
        android:icon="@drawable/ic_watchicon_30dp"
        android:title="Watch List"
        android:checked="false"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/navigation_profile"
        android:icon="@drawable/ic_profileicon_24dp"
        android:title="Profile"
        android:checked="false"
        app:showAsAction="ifRoom"/>

</menu>
